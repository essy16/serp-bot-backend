version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
    environment:
      - mysql -u doadmin -pAVNS_UvD8NRS3PQSeGk2sRiq -h client-job-db-do-user-20060435-0.l.db.ondigitalocean.com -P 25060 < 
    volumes:
      - ./app:/app

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Essy5036!
      MYSQL_DATABASE: clients_jobs_db
    ports:
      - "3306:3306"
