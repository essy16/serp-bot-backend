version: '3.9'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
      - ./certs:/certs
    environment:
      - DB_HOST=client-job-db-do-user-20060435-0.l.db.ondigitalocean.com
      - DB_PORT=25060
      - DB_NAME=clients_jobs_db
      - DB_USER=doadmin
      - DB_PASSWORD=AVNS_UvD8NRS3PQSeGk2sRiq
      - SSL_CA=/certs/ca-certificate.crt
